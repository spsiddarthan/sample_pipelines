jobs:
  - name: box-man
    type: manifest
    steps:
      - GET: box-nginx
      - GET: trigger-dv-man

  - name: dcl-upgrade-deploy
    type: dclDeploy
    steps:
      - GET: box-man
      - GET: dcl-cluster
